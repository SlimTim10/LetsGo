import React, { Component } from 'react';

class Suggestion extends Component {
  render() {
    return (
      <section className="suggestion">
        <button className="btn-suggestion">New suggestion</button>
      </section>
    );
  }
}

export default Suggestion;
